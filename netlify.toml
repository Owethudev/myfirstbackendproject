[build]
  publish = "."

[build.environment]
  # Ensure Vite uses the correct base when deploying (optional)
  # Uncomment and set if you need a custom base URL
  # VITE_API_BASE_URL = "https://snpl-port.onrender.com"

[[redirects]]
  from = "/**"
  to = "/index.html"
  status = 200
